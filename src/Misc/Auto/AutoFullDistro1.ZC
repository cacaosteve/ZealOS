U0 Auto()
{
	WinMax;
	// Softboot ΓÇö do not Reboot through BootMHD Selection (AUTO.ISO MBR blocks).
	Del("C:/Home/AutoISOStage1.DD",, FALSE, FALSE);
	FileWrite("C:/Home/AutoISOStage2.DD", "2", 1);
	"AutoISO stage 1 ok; BootRAM to stage 2.\n";
	BootRAM("C:/Boot/Kernel.ZXE");
}
Auto;
