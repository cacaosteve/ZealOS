Cd(__DIR__);;
#include "Load"
/* This branch is testing the Mac mini through macOS Internet Sharing.
   Skip the generic DHCP retry loop: it can occupy the terminal for two
   minutes and adds unrelated RX state before the fixed-address test. */
#include "StartShare"
